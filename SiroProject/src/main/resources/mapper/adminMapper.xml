<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seschool.bank.dao.AdminMapper">

	<select id="selectOne" parameterType="Board" resultType="Board">
		Select *
		From QNABOARD
		where boardno = #{boardno}
	</select>

	<!-- RowBounds를 사용하는 Case -->
	<select id="selectAll" resultType="Board" parameterType="map">
		SELECT
		BOARDNO,
		CUSTID,
		TITLE,
		CONTENT,
		to_char(regdate, 'YYYY-MM-DD') as
		regdate,
		ORIGINALFILE,
		SAVEDFILE,
		HITCOUNT,
		REPLYTEXT,
		REPDATE
		FROM QNABOARD
		<if test="searchItem != null">
			WHERE ${searchItem} LIKE '%'||#{searchWord}||'%'
		</if>
		ORDER BY BOARDNO DESC
	</select>

	<select id="getBoardCount" parameterType="map" resultType="int">
		SELECT count(*)
		FROM QNABOARD
		<if test="searchItem != null">
			WHERE ${searchItem} LIKE '%'||#{searchWord} || '%'
		</if>
	</select>

	<select id="listLoan" parameterType="Account"
		resultType="Account">
		SELECT accountno, custid, accounttype, balance, payment, interest,
		to_char(startdeposit,'YYYY-MM-DD') as startdeposit,
		to_char(ENDDEPOSIT,'YYYY-MM-DD') as ENDDEPOSIT,APPROVAL
		FROM account
		WHERE accounttype = 'loan'
		And
		APPROVAL = 'N'
		ORDER BY accountno
	</select>

	<update id="loanOK" parameterType="Account">
		update ACCOUNT set
		APPROVAL = 'Y'
		where accountno = #{accountno}
	</update>


	<update id="loanX" parameterType="Account">
		Delete From ACCOUNT
		where accountno = #{accountno}
	</update>
	
	<delete id="qnadelete" parameterType="int">
		Delete From QNABOARD
		where boardno = #{boardno}
	</delete>
</mapper>