<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="global.seshool.bank.dao.BoardDAO">

	<insert id="insert" parameterType="Account" >
		INSERT INTO Account
		(
			 accountno,
			 custid,
			 accounttype,
			 balance,
			 payment,
			 interest,
			 startdeposit,
			 enddeposit,
			 term,
			 approval
		)
		VALUES
		(
			ACCOUNTLOG_SEQ.nextval
			, #{custid}
			, #{accounttype}
			, #{balance}
			, #{payment}
			, #{interest}
			, #{startdeposit}
			, #{enddeposit}
			, #{term}
			, #{approval}
		)
	</insert>
	
	<select id="selectOne" parameterType="Account" resultType="Account">
	Select * From Account
	where custid = #{custid}
	</select>

	<select id="selectAll" resultType="Account" parameterType="Account">
		SELECT
			 accountno,
			 custid,
			 accounttype,
			 balance,
			 payment,
			 interest,
			 startdeposit,
			 enddeposit,
			 term,
			 approval
		FROM Account
		ORDER BY accountno DESC
	</select>	

</mapper>